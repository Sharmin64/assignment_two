import { Request, Response } from 'express';

const cteateProduct = (req: Request, res: Response) => {
  const product = req.body;

  // will call service func to send this data

  // will send response from here
};
